#commit message:
$CommitMessage = "Added some powershell scripts to automate workflow"

#add all files to commit:
git add .

#add new commit and message to it:
git commit -m $CommitMessage

#push newly created commit to github:
git push